#!/sbin/openrc-run

description="Deploy Fruit configurations."

depend() {
	before crond ntpd sshd docker
	after net root bootmisc modules syslog sysctl
}

start() {
	/usr/sbin/fruit-deploy
	return $?
}

stop() {
	return 0
}
